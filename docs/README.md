# eu-doc 文档目录

本目录用于保存项目文档。AI 默认不需要完整读取整个 `docs/`，应先读取根目录的 `AGENTS.md` 和 `CURRENT_STATUS.md`，再按任务类型读取相关文档。

## 目录说明

- `guides/`：正式指南，例如开发访问、搜索页多语言说明。
- `legal/`：法律和合规文档，例如隐私政策、服务条款。
- `ai-guides/`：AI 工作补充说明。根目录 `AGENTS.md` 和 `CURRENT_STATUS.md` 仍是默认入口。
- `archive/`：历史文档归档。AI 不应默认读取，除非需要追溯历史。
